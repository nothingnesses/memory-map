mutation UpdateS3ObjectMutation(
	$id: ID!
	$name: String!
	$made_on: String
	$location: LocationInput
	$publicity: PublicityOverride!
) {
	updateS3Object(
		id: $id
		name: $name
		madeOn: $made_on
		location: $location
		publicity: $publicity
	) {
		id
		name
		madeOn
		location {
			latitude
			longitude
		}
		publicity
	}
}
