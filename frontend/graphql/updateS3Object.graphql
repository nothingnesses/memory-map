mutation UpdateS3ObjectMutation(
	$id: ID!
	$name: String!
	$made_on: String
	$location: LocationInput
	$publicity: PublicityOverride!
	$allowedUsers: [String!]
) {
	updateS3Object(
		id: $id
		name: $name
		madeOn: $made_on
		location: $location
		publicity: $publicity
		allowedUsers: $allowedUsers
	) {
		id
		name
		madeOn
		location {
			latitude
			longitude
		}
		publicity
		allowedUsers
	}
}
